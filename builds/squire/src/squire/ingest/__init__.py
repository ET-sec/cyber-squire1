"""Squire ingest subpackage.

Modules:
  chunker      -- splits markdown docs into header-aware chunks with metadata.
  grc_indexer  -- embeds chunks via the configured provider and upserts into ir_chunks.
"""
