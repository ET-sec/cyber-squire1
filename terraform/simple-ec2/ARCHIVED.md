# ARCHIVED: AWS simple-ec2 (frozen 2026)

This directory is **no longer active**. The AWS account was suspended in 2026
and the EC2 instance this config managed is gone. These files are kept as a
**frozen reference** for the AWS-era design, not as live infrastructure.
Do not `terraform apply` here.

**Active infrastructure lives in `../cd-oci-infrastructure/`** (Oracle Cloud,
Always Free ARM).

Why keep it: it documents the earliest single-instance EC2 layout the stack
started on, so the lineage from AWS to DigitalOcean to OCI stays traceable.
