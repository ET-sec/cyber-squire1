# ARCHIVED: AWS automation infrastructure (frozen 2026)

This directory is **no longer active**. The AWS account was suspended in 2026,
so nothing here maps to running infrastructure anymore. These files are kept
as a **frozen reference** for the AWS-era production design, not as live
infrastructure. Do not `terraform apply` here.

**Active infrastructure lives in `../cd-oci-infrastructure/`** (Oracle Cloud,
Always Free ARM).

Why keep it: it documents the full AWS-era automation stack (instance, network,
IAM) so the design record survives even though the account did not.
